initSidebarItems({"struct":[["EsRuntime","the EsRuntime is a facade that adds all script todo's to the EsRuntimes's event queue so they are invoked in a single worker thread you can wait for those tasks to complete by calling the _sync variants of the public methods here"],["EsScriptCode",""]],"type":[["ModuleCodeLoader","A ModuleCodeLoader function is used to load code into the runtime The first argument is the (relative) path of the module to import The second argument is the absolute path to the module which is importing the new module (reference_path) the EsScriptCode struct which is returned should allways contain an absolute path even if the module is loaded with a relative path"]]});